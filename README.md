# GBG-BODYPARTS Catalog

Internal web catalog for `GBG-BODYPARTS` parts (brand → model → parts browse, search, part
detail). The app lives in [`gbg-catalog/`](gbg-catalog/) — see
[`gbg-catalog/README.md`](gbg-catalog/README.md) for setup and run instructions.

See [`CLAUDE.md`](CLAUDE.md) for project/database context.
